# Backend Environment Variables Template# Example DATABASE_URL values

# Copy this file to .env and fill in your actual values# Local Postgres running on host (Windows/macOS/Linux):

# DATABASE_URL=postgresql://reader:readerpass@localhost:5432/readingtracker

# Database Configuration

DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require# When running backend inside Docker Compose (service host 'postgres'):

# DATABASE_URL=postgresql://reader:readerpass@postgres:5432/readingtracker

# Example for local PostgreSQL:

# DATABASE_URL=postgresql://reader:password@localhost:5432/readingtracker#Azure PostgreSQL

# Example for Azure PostgreSQL:
# DATABASE_URL=postgresql://username:password@servername.postgres.database.azure.com:5432/readingtracker?sslmode=require

# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development
FLASK_DEBUG=1